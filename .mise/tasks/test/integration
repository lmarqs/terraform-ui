#!/usr/bin/env bash
#MISE description="Test against real terraform/tofu/terragrunt binaries. Run when changing service layer or CLI."

exec go test -tags integration -timeout 180s ./tests/integration/...
