---
allowed-tools: Bash(mise run:*)
description: Bootstrap all project dependencies (mise run setup)
---

## Mise task: `setup`

Run `mise run setup` to install all dependencies (node, python, ruby, go tools).
