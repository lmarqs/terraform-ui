---
allowed-tools: Bash(mise run:*)
description: Run macro tape tests to verify TUI rendering (mise run test:macro)
---

## Mise task: `test:macro`

Run `mise run test:macro` to execute macro tape tests against the built binary.

Required after any changes to `View()`, layout, or navigation.

Related commands: /build, /test, /run
