---
allowed-tools: Bash(mise run:*), Bash(gofmt:*)
description: Format Go source files (mise run go:fmt)
---

## Mise task: `go:fmt`

Run `mise run go:fmt` to format all Go source files with gofmt.

This runs automatically as part of `mise run go:build`.

Related commands: /go-build, /go-lint
