---
allowed-tools: Bash(mise run:*), Bash(go test:*), Bash(go tool:*)
description: Run Go coverage report (mise run go:coverage)
---

## Mise task: `go:coverage`

Run `mise run go:coverage` to generate a coverage report.

Outputs coverage.out and prints per-function coverage to stdout.

Related commands: /go-test
