---
allowed-tools: Bash(mise run:*), Bash(go test:*)
description: Run Go unit tests (mise run test)
---

## Mise task: `test`

Run `mise run test` to execute all Go unit tests.

For a single package: `go test ./internal/terraform/...`

Related commands: /build, /coverage
